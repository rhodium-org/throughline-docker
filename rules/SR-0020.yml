uid: SR-0020
type: system_requirement
status: approved
title: Use CMD for default arguments
text: >-
  Use CMD to provide the default command or default arguments; combine it with ENTRYPOINT
  when the image runs a fixed executable with variable arguments.
links:
- target: UR-0008
  type: implements
attrs:
  source_ref: "Docker Dockerfile best practices: CMD — Default Arguments"
